# Environment: "dev" or "prd" (optional - defaults to prd if not set)
# Set to "dev" to use BOT_TOKEN_DEV, otherwise uses BOT_TOKEN (production)
ENV=dev

# Production token (get from @BotFather on Telegram)
# This is used when ENV is not set or ENV=prd (backward compatible)
BOT_TOKEN=your_production_bot_token_here

# Development token (optional, used when ENV=dev)
BOT_TOKEN_DEV=your_development_bot_token_here
